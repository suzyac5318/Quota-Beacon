# 开发历程与变更日志

本文件记录本仓库可核实的版本与开发里程碑。未创建 Git 提交或标签的工作统一标记为“未发布”，不追溯虚构版本号。

## 1.9.0 — 2026-08-12

### 功能

- 账号窗口外壳跟随当前激活 Codex 账号的真实 5 小时剩余额度与用户调色盘，复用主悬浮卡的轻量主题色；额度未知时保持中性玻璃色。
- 账号窗口与主悬浮卡统一为 `13.5px` 模糊、`1.18` 饱和度、半像素内描边和透明控制面材质；账号列表仍保持中性，当前账号行仅使用弱状态染色。
- 从主卡或系统托盘打开账号窗口时均传递同一主题；账号切换后等待新账号真实额度刷新，再同步更新窗口色调。

### 验证

- 前端测试 `41/41`、TypeScript/Vite 构建和 `cargo check` 通过。
- 在浏览器检查中性色、`60%` 浅绿色和 `0%` 淡红色；真实 Windows Tauri 首轮验收发现 `320×180` 在非整数缩放下会裁切第二个账号的周额度，因此将紧凑高度调整为 `320×190`。
- 高度修复后重新通过 Rust 测试 `28/28` 和 Windows `tauri build --no-bundle`；构建产物与安装文件 SHA-256 一致，v1.8.2 备份保留，v1.9.0 进程从正式安装路径启动并正常响应。
- 用户已在真实 Windows Tauri 双窗口中确认账号色调、透明模糊和第二个账号周额度完整显示，视觉验收通过。

## 1.8.2 — 2026-08-11

### 修复

- 将 Windows 原生调色盘模糊层的透明度、缩放和垂直位移与调色盘外壳动画同步，消除展开时模糊层提前铺满、收回时模糊层滞留造成的视觉割裂。
- 分别匹配色彩预览与颜色编辑器的进入延迟、退出延迟和动画时长；关闭窗口前等待完整收回动画结束。

### 验证

- 前端测试 `40/40`、Rust 测试 `28/28`、TypeScript/Vite 构建、`cargo check`、Windows `tauri build --no-bundle` 及 `git diff --check` 通过。
- 已替换并启动本机安装版；构建产物与安装文件 SHA-256 一致，并以 60fps 实录逐帧确认调色盘展开/收回期间未再出现独立模糊层提前铺满或滞留。

## 1.8.1 — 2026-08-11

### 修复

- 账号切换成功提示显示 10 秒后自动消失；后续错误或登录提示不会被旧定时器误清除。

### 验证

- 账号窗口定向测试 `4/4`、TypeScript/Vite 构建及 `git diff --check` 通过。

## 1.8.0 — 2026-08-11

### 功能

- 在每个已保存账号下方显示该账号的每周剩余额度；不显示 5 小时额度、相对更新时间或过期时间标签。
- 当前账号复用主悬浮卡每 10 秒更新的真实额度快照；非当前账号使用其本机 DPAPI 加密凭据独立查询，并严格缓存 5 分钟，不切换或覆盖 Codex 当前 `auth.json`。
- 单个账号凭据失效时只影响该账号，显示重新登录入口并保留其他账号的额度和切换操作。

### 验证

- 新增账号周额度显示、百分比四舍五入、不出现 5 小时/相对时间文案，以及非当前账号 5 分钟缓存边界的回归测试。
- 通过前端 `40/40`、Rust `26/26` 测试、TypeScript/Vite 构建、`cargo check`、`git diff --check` 和 Windows `tauri build --no-bundle`。
- 在浏览器 `320×180` 实际账号窗口尺寸下确认两行账号、周额度和操作按钮完整显示；安装版替换与真实 Tauri 双窗口验收尚未执行。

## 1.7.10 — 2026-08-11

### 修复

- 切换成功提示出现时，为账号浮窗额外增加 `24px` 原生窗口高度，避免提示通过压缩列表遮挡账号标签；输入表单同时展开时也保留独立提示空间。
- 移除账号行中的“切换并重启”按钮，只保留普通“切换”操作；切换提示继续说明重启 Codex 后完整生效。

### 验证

- 新增切换提示触发窗口增高、账号窗口不再渲染“切换并重启”按钮的前端回归测试。

## 1.7.9 — 2026-08-11

### 修复

- 根据真实 Windows Tauri 截图，将账号浮窗折叠高度由 `160px` 调整为 `180px`，确保新增账号输入框未展开时至少完整显示两个账号标签。
- 继续保留折叠表单与空通知行 `0` 间距的布局规则；增加的高度只用于账号列表，不恢复隐藏输入区占位。

### 验证

- 更新紧凑窗口配置、前端桥接常量和原生重置尺寸的回归检查，并按 `320×180` 与 `320×240` 验证折叠/展开布局。

## 1.7.8 — 2026-08-11

### 修复

- 修复子 Agent 与分叉会话日志内嵌父会话元数据时，被错误合并到父会话并漏计 Token 的问题。
- 每个日志只采用首个 `session_meta` 作为自身会话 ID；同一会话文件在归档目录间移动时仍按 ID 去重。

### 验证

- 新增分叉日志身份回归测试，并保留归档迁移、增量读取和累计值去重测试。
- 使用本机现有日志独立全量核算，确认修复前约 `3.528B`、修复后约 `3.838B`，恢复约 `309.9M` Token；活跃会话会使数值继续增长。

## 1.7.7 — 2026-08-11

### 修复

- 将账号浮窗折叠高度由 `180px` 收紧为 `160px`，让新增账号输入框未展开时不再保留会遮挡下方内容的透明原生窗口区域。
- 移除折叠表单和空通知行残留的网格间距；展开时再随表单动效恢复必要间距，账号列表与窗口边界保持同步。

### 验证

- 更新账号浮窗紧凑高度、原生窗口重置尺寸和折叠布局间距的回归检查。

## 1.7.6 — 2026-08-11

### 界面

- 将账号浮窗的默认高度由 `240px` 收窄为 `180px`，折叠状态不再保留下方大块空白。
- 点击加号后，窗口高度以可中断的 ease-out 插值平滑扩展至 `240px`，新增账号表单随后淡入并轻微下移到位；折叠时执行反向动效。
- 减少动态效果模式下直接切换窗口尺寸和表单状态，不播放非必要动画。

### 验证

- 新增紧凑/展开高度、窗口尺寸权限、原生重新定位、表单折叠动效与焦点状态的回归测试。

## 1.7.5 — 2026-08-11

### 修复

- 右上角加号现在可以在新增账号表单的展开与折叠状态之间切换；折叠时清空未提交的账号名称。
- 每次账号浮窗重新打开时，由原生窗口事件重置新增表单，避免窗口隐藏后仍保留上一次展开状态。

### 验证

- 新增加号二次点击折叠、重新打开浮窗重置表单及原生重置事件连接的回归测试。

## 1.7.4 — 2026-08-11

### 界面

- 将账号浮窗右上角关闭图标改为新增账号的加号入口。
- 账号名称输入框和添加按钮默认隐藏，仅在用户点击加号后展开并自动聚焦输入框；账号保存或登录完成后自动收起。

### 验证

- 新增账号表单默认隐藏、点击加号展开及无隐式保存行为的前端回归测试。

## 1.7.3 — 2026-08-11

### 界面

- 将不同账号行的重命名、删除图标固定在相同网格列，避免因条件操作按钮数量不同而左右错位。
- 隐藏账号列表的可见滚动条，同时保留鼠标滚轮、触控板和键盘滚动能力。

### 验证

- 新增账号操作图标列定位与隐藏滚动条样式的回归测试。

## 1.7.2 — 2026-08-11

### 修复

- 账号浮窗展开后，再次点击主卡左上角账号胶囊即可收回。
- 账号浮窗展开时，点击主卡其他区域或 Quota Beacon 之外的任意区域会自动收回；浮窗内部交互保持可用。

### 验证

- 新增账号入口二次点击、主卡其他区域点击及应用失焦关闭的前端回归测试。

## 1.7.1 — 2026-08-11

### 界面

- 将系统托盘右键菜单及 Codex 账号子菜单的可见文案统一为中文。

## 1.7.0 — 2026-08-11

### 新增

- 新增 Windows Codex 多账号本地保险库：用户显式确认后，使用当前 Windows 用户作用域的 DPAPI 加密保存文件型登录凭据；前端只接收自定义名称、脱敏邮箱和状态。
- 新增展开卡片账号胶囊和系统托盘账号子菜单双入口，以及独立 `320×240` 账号管理窗口；支持保存当前账号、隔离官方登录添加账号、取消/超时、重命名、删除、重新登录和快捷切换。
- 新增“切换并重启 Codex”独立操作；执行前必须二次确认，并明确提示可能中断正在运行的任务。

### 安全与可靠性

- 切换前同步当前账号刷新后的凭据；目标凭据经解密、JSON 和账号指纹校验后，才在 `auth.json` 同目录原子替换。额度请求按账号代次隔离，切换后旧账号的在途响应不得写回缓存或界面。
- 为切换保留 DPAPI 加密的 last-known-good 恢复点；本地替换或复核失败时恢复原登录。额度网络刷新失败与本地凭据切换结果独立报告。
- 拒绝符号链接、目录、超大文件、无效 JSON 和账号指纹不匹配；隔离登录目录仅在应用控制目录中创建和清理，不读取浏览器 Cookie，不记录或向前端暴露 Token。

### 界面与兼容

- 账号窗口跟随主窗口，并按显示器物理工作区在上下方避让；不参与窗口状态持久化，支持键盘焦点和减少动态效果设置。
- 普通切换立即更新活动账号并清空上一账号额度，卡片显示加载态后异步呈现新账号额度，不再等待旧请求或短暂显示旧账号数据。
- Token 历史仍按本机全部 Codex 会话累计，不按账号重新归属；保留现有 10 秒额度刷新、101 级配色、色卡预览、折叠/展开、高 DPI 与透明窗口行为。

### 验证

- 新增 DPAPI 明文隔离、重复账号、双账号切换、无效/超大凭据、唯一恢复账号删除保护、切换后元数据提交失败回滚，以及账号胶囊和显式保存流程测试。
- 新增网络中断映射测试，确认离线时返回可重试的 `unavailable` 状态，不把网络失败误报为本地切换失败。
- 浏览器按主卡 `320×320`、账号窗口 `320×240` 检查单账号和双账号布局，未发现溢出、圆角裁切或控件遮挡。

## 1.6.11 — 2026-08-11

### 修复

- 为额度请求增加单一在途请求保护：自动刷新、手动刷新、窗口恢复和焦点事件会复用同一个 Promise，不再向 Rust 端堆积重复请求。
- 正常成功状态继续每 `10s` 强制刷新；连续失败按 `30s`、`60s`、`120s` 退避并封顶，成功或手动刷新成功后恢复正常周期。
- 托盘“立即刷新”可绕过退避，系统恢复和窗口重新激活仍遵守退避；鼠标移入只展示内存中的最新快照，不再触发接口请求。
- Rust 强制刷新在已有请求运行时直接返回当前缓存；没有缓存时返回安全的“正在刷新”状态，不再等待互斥锁形成请求队列。

### 验证

- 新增刷新策略单元测试，覆盖成功后的 `10s` 周期、三级失败退避与 `120s` 上限。

## 1.6.10 — 2026-08-11

### 修复

- 将 Windows Codex 窗口枚举由固定 `80ms` 高频循环改为分层轮询：Codex 前台 `500ms`、后台 `1s`、未运行 `2s`，降低常驻 CPU 开销。
- 缓存当前 Codex 日志路径，仅每 `5s` 检查一次日志轮转；当前对话 Token 在前台每 `2s` 更新，后台每 `10s` 更新，并将全量会话目录发现限制为每 `10s` 一次。
- 为持续增长的 Codex 会话 JSONL 增加追加式解析，只读取上次已处理位置之后的新内容；文件缩短或原地改写时仍安全回退到完整解析。

### 验证

- 新增会话文件增长回归测试，确认追加 Token 记录能够从缓存位置继续更新，同时保留跨 `sessions`、`archived_sessions` 与 `session-archive` 的累计值和去重行为。

## 1.6.9 — 2026-08-11

### 调整

- 在保持 `v1.6.8` 表面透明度、主题染色、Aurora、饱和度、描边和现有图层结构不变的前提下，将三个窗口共用的原生高斯模糊从 `6.0` 降至 `4.5`。
- 主卡片、色彩预览和调色盘的 CSS 模糊同步由 `18px` 降至 `13.5px`；折叠圆球由 `16px` 降至 `12px`，圆球强调状态由 `18px` 降至 `13.5px`，整体再减弱约 25%。

### 验证

- 更新三窗口原生模糊、三张展开卡和折叠圆球的统一模糊回归断言，并保留 `v1.6.8` 透明表面断言。
- 前端 26 项测试、Rust 17 项测试、TypeScript/Vite 构建、Rust 检查、Git 差异检查及 Windows Tauri release 构建通过。

## 1.6.8 — 2026-08-11

### 调整

- 主卡片、色彩预览和调色盘继续使用各自现有的原生/CSS 承载层；三个窗口的原生高斯模糊统一保持 `6.0`，三个展开卡片的现有 CSS 模糊统一由 `24px` 降至 `18px`，不新增图层。
- 降低主卡片、折叠圆球和两个控制卡片的现有透明白表面、主题染色与 Aurora 不透明度，让桌面内容更明显，同时保留背景模糊、关键配色、内部描边和文字可读性。

### 验证

- 更新三卡统一模糊和透明表面回归断言；前端 26 项测试、Rust 17 项测试、TypeScript/Vite 构建、Rust 检查及 Windows Tauri release 构建通过。
- 浏览器按主卡 `320×320`、两个控制卡 `320×104` 检查，布局、圆角和文字可读性正常；真实桌面通透感继续以安装后的 Windows 原生窗口验收。

## 1.6.7 — 2026-08-11

### 调整

- 仅将三个现有 Windows Host Backdrop 原生模糊面的高斯模糊参数从 `8.0` 降至 `6.0`，让插件后方内容稍微更明显，同时继续保持模糊。
- 保留现有模糊层、圆角裁切、边缘内缩、颜色、透明度、描边、尺寸和窗口结构，不新增任何承载层。

### 验证

- 同步原生模糊参数回归断言；源码差异确认未新增或重排任何原生/CSS 承载层。
- 前端 25 项测试、Rust 17 项测试、TypeScript/Vite 构建、Rust 检查及 Windows Tauri release 构建通过。

## 1.6.6 — 2026-08-10

### 修复

- 将主卡片、折叠圆球和两个控制卡片的高对比白色内描边从 `1px` 调整为半像素覆盖层；在当前非整数 WebView 像素倍率下保持接近一个物理像素，消除圆弧边缘周期性叠亮形成的凸点。
- 两个下方控制卡片继续使用与主卡片相同的 `24px` CSS 模糊、饱和度参数和独立 Windows Host Backdrop 模糊面。

### 验证

- 新增半像素统一内描边回归断言，禁止恢复会在非整数倍率下变粗的 `1px` 内描边。
- Chromium 主卡预览的四个圆角轮廓连续；前端 26 项测试、Rust 17 项测试、TypeScript/Vite 构建、Rust 检查、Git 差异检查及 Windows Tauri release 构建通过。

## 1.6.5 — 2026-08-10

### 修复

- 主窗口的 WebView 像素比例改为使用客户区物理宽度计算，不再把 Windows 透明窗口的不可见外框计入比例；原生模糊面由此与 CSS 卡片保持同一尺寸和圆角，消除折叠及展开状态角落露出的凸起。
- 保留 v1.6.4 为两个下方控制卡片新增的独立 Host Backdrop 模糊面、统一模糊参数和内部白色描边。

### 验证

- 新增客户区尺寸回归断言，禁止像素比例计算回退到 `outerSize()`。
- 前端 26 项测试、Rust 17 项测试、TypeScript/Vite 构建、Rust 检查、Git 差异检查及 Windows Tauri release 构建通过。

## 1.6.4 — 2026-08-10

### 新增

- 将主卡片使用的 Windows `8px` Host Backdrop 轻模糊扩展到色彩预览窗和调色盘窗；三个透明 Tauri 窗口各自使用独立的圆角合成面，并在显示、移动、缩放和关闭时同步跟随。
- 控制窗使用与主卡片一致的 CSS 模糊与饱和度参数，保留原有颜色、透明度、尺寸、布局和交互。

### 修复

- 将主卡片、折叠圆球和两个控制卡片的白色轮廓统一为完全绘制在卡片内部的单层描边，移除会在圆角切线处叠亮的实体边框、顶部高光和控制窗外阴影。
- 原生模糊面继续收在内部描边以内，避免独立的 Win32 与 WebView 抗锯齿边缘再次叠加。

### 验证

- 新增控制窗原生模糊几何、三窗口生命周期和统一内描边回归测试。
- 前端 26 项测试、Rust 17 项测试、TypeScript/Vite 构建、Rust 检查、Git 差异检查及 Windows Tauri release 构建通过。

## 1.6.3 — 2026-08-09

### 修复

- 将 Windows 原生模糊承载面内缩到 CSS `1px` 描边以内，避免两套独立抗锯齿圆角在高 DPI 缩放下叠出亮色凸点和不连续白边。
- 原生模糊面的圆角半径同步扣除描边宽度，由 WebView 单独绘制最外层轮廓；模糊强度、卡片尺寸、CSS 圆角、颜色和交互保持不变。

### 验证

- 新增并更新展开、折叠、动画中间态及非整数缩放下的模糊面边界回归测试。
- 前端 25 项测试、Rust 16 项测试、TypeScript/Vite 构建、Rust 检查及 Windows Tauri release 构建通过。

## 1.6.2 — 2026-08-08

### 新增

- 在 `v1.6.1` 的浅绿色透明外观上增加无染色的轻微背景模糊；颜色、透明度、圆角、尺寸和交互保持不变。
- 使用启用了 `DWMWA_USE_HOSTBACKDROPBRUSH` 的独立圆角合成面承载 `8px` 轻模糊，并置于原窗口正后方；不再给矩形主窗口附加系统 Acrylic 或 Blur 底色。
- 修正折叠态缩放定位，只计算一次 `10px` 内缩，避免模糊面与 `80×80` 圆球错位重影。
- 模糊面改为按卡片真实的 `80×80 ↔ 320×320` 几何插值，不再从已经放大的父窗口尺寸推算；展开沿用 `400ms` Morph，收起与卡片一致地等待 `90ms` 后运行 `190ms`，并共享同一组弹性关键帧。
- 折叠态改用 WebView 实测的 CSS 到物理像素比例计算 `80×80` 卡片和 `10px` 内缩，并对原生像素取整采取向内保护，修正页面缩放下模糊面偏小及左上圆角外露。

### 验证

- 前端 25 项测试、Rust 16 项测试、TypeScript/Vite 构建、Rust 检查、Git 差异检查及 Windows Tauri release 构建通过。
- 用户完成真实 Windows 视觉验收；安装文件与 Release 构建 SHA-256 一致，正式安装进程可响应，运行日志确认原生模糊层成功附加并使用 WebView 实测像素比例。

## 1.6.1 — 2026-08-07

### 修复

- 停用作用于矩形 HWND 的 Windows DWM 背景材质与窗口 Region，恢复圆角轮廓外的逐像素透明。
- 删除展开卡片、折叠圆球、悬停状态和备用圆球的 CSS 外阴影，消除透明窗口边界内残留的浅灰色方形区域。
- 保留现有卡片透明表面、额度染色、内部高光、布局、尺寸和交互行为。

### 验证

- 新增原生矩形材质禁用和透明窗口外阴影回归测试。
- 前端测试、TypeScript/Vite 构建、Rust 检查、Windows Tauri release 构建、安装文件哈希匹配与 Windows 真机视觉验收通过。

## 1.6.0 — 2026-08-06

### 新增

- 为 Windows 11 的主窗口、色卡预览窗和色卡编辑窗启用 DWM Desktop Acrylic，实现真实桌面背景模糊。
- 使用 DPI-aware 原生圆角区域裁切 DWM 材质，并让主窗口裁切随展开与折叠动画可中断地同步，消除圆角外灰色矩形。
- 将额度颜色调整为独立的淡色染色与光晕层，保留 101 级颜色状态，同时使用中性玻璃、细亮边框和高对比文字。
- 将五个关键色标对齐到 `0% / 20% / 35% / 60% / 100%`，确保红、橙、黄、浅绿、绿准确落点。
- 增加减少透明度、增强对比度和减少动态效果的无障碍降级样式。

### 验证

- 新增原生透明窗口、DWM 材质、玻璃层级和辅助功能回归测试。

## 1.5.5 — 2026-07-26

### 修复

- 历史累计 Token 扫描新增 `.codex/session-archive`，避免 Codex 将会话迁移到二级归档后累计值下降。
- 新增会话文件迁移回归测试，确保迁移前后的累计 Token 和会话数量保持一致。

### 验证

- Rust 单元测试覆盖 `sessions` 到 `session-archive/stage2/...` 的迁移场景，迁移前后的累计 Token 和会话数量断言通过。
- 前端测试、TypeScript/Vite 构建、Rust 检查、Windows Tauri release 构建与 Git 差异检查通过。

## 1.5.4 — 2026-07-17

### 文档

- 按用户要求删除已被两张新产品图替换、且不再被仓库引用的旧 `docs/images/quota-states.png`。
- 删除前已在忽略提交的 `work/backups/` 中创建可恢复备份，并记录原路径、目的、备份路径、时间与 SHA-256。

### 验证

- 删除前原文件与备份 SHA-256 一致；README 中两张新图片路径有效。
- 前端测试、TypeScript/Vite 构建、Rust 检查与 Git 差异检查通过。

## 1.5.3 — 2026-07-17

### 文档

- 将 README 首页原有的三状态截图替换为新的产品主视觉和 101 级额度配色系统展示图。
- 保留原始展示图片文件，避免破坏历史引用。

### 验证

- 两张新图片与用户提供的源文件 SHA-256 完全一致，README 路径有效。
- 前端测试、TypeScript/Vite 构建、Rust 检查与 Git 差异检查通过。

## 1.5.2 — 2026-07-17

### 维护

- 将 GitHub 官方 checkout、Node 环境和工件上传/下载 Actions 升级到基于 Node.js 24 的当前主版本，并继续固定到完整提交 SHA，消除 Node.js 20 弃用警告。

### 验证

- `v1.5.1` 主分支 CI 的前端、Windows 构建、macOS Universal 构建、ad-hoc 签名、双架构与 DMG 校验全部通过；本补丁再次由主分支 CI 验证新版 Actions。

## 1.5.1 — 2026-07-17

### 开源发布

- 更新项目首页、安装步骤、平台验证状态、真实截图、非官方声明和私密安全报告入口。
- 首次推送流程改为分别推送 `main` 与单个版本标签，避免历史标签批量触发 Release。
- Release 改为等待 Windows 与 macOS 构建全部成功后，在单一草稿中汇总 Windows ZIP、macOS Universal ZIP、DMG 和 SHA-256。
- GitHub Actions 权限按最小范围设置，所有外部 Actions 固定到完整提交 SHA，并新增 Dependabot、CODEOWNERS 与安全 Issue 引导。
- MIT 版权声明同时保留上游贡献者与 `suzyac5318 / Quota Beacon contributors`。

### 验证范围

- Windows 11 为已完成实机验证的平台。
- macOS Universal 包由 GitHub Actions 构建并自动验证双架构、ad-hoc 签名与 DMG 完整性；真实 Mac 桌面交互仍明确标记为待验收。

## 1.5.0 — 2026-07-17

### 新增

- macOS Universal 构建改用无需付费开发者账号的 ad-hoc 签名，同时保留 Apple Silicon 与 Intel 双架构。
- Release 直接提供 DMG、完整 bundle ZIP 和 SHA-256 校验文件。
- 新增 macOS 自动验收脚本，检查递归签名、ad-hoc 身份、双架构主程序和 DMG 完整性。

### 变更

- CI 与 Release 工件统一改名为 `quota-beacon-macos-universal-ad-hoc`，明确其未公证的分发边界。
- 安装说明补充 Gatekeeper 的安全放行流程、校验值用法和 Mac 实机验收要求。

### 验证

- 前端测试、TypeScript/Vite 构建、Rust 检查和 Windows Tauri 构建通过。
- macOS Universal 架构、ad-hoc 签名与 DMG 校验由 macOS CI 执行；真实 Mac 首次安装与桌面交互待发布工件生成后验收。

## 1.4.3 — 2026-07-17

### 修复

- 收窄色彩预览标题区的说明文字样式选择器，避免动态百分比数字被错误缩小为 `10px`，使数字与百分号统一保持清晰的 `20px` 显示。

### 验证

- 新增 CSS 选择器回归测试，防止说明文字样式再次影响动态百分比。

## 1.4.2 — 2026-07-14

### 变更

- 将应用图标改为透明背景的明亮玻璃质感「Q」轨道，采用蓝、靛、紫、粉多彩渐变与柔和外发光。
- 重新生成 Windows、macOS、iOS 与 Android 所需的全部应用图标尺寸。

## 1.4.1 — 2026-07-14

### 优化

- 将卡片背景 Morph 与文字动画彻底分离，小窗百分比和完整卡片内容保持独立且始终挂载，文字只使用透明度、位移和轻微模糊过渡。
- 展开时背景先行，小窗与大卡片百分比交叉淡入淡出，标题、累计使用、进度条和底部信息按约 `30ms` 错峰出现。
- 收回时次要信息先退出，大百分比向小窗锚点轻移后与小窗百分比交叉切换，避免文字随容器收缩而挤压、换行或跳变。
- 快速移入移出可自然反向播放；保留现有卡片背景 Morph、视觉样式和减少动态效果支持。

### 验证

- 新增组件结构测试，确认折叠与展开内容在方向反转时不会重复挂载。
- 前端测试、TypeScript/Vite 构建、Rust 检查、Windows Tauri release 构建及真实桌面交互验证通过。

## 1.4.0 — 2026-07-13

### 新增

- 主悬浮卡片在“累计使用”上方新增“当前窗口使用量”，自动显示当前 Codex 对话窗口累计 Token，缺失时显示 `null`。

### 变更

- 删除独立的当前窗口 Token 浮窗及其窗口创建、定位和拖动逻辑，当前窗口数据改为直接发送至主悬浮卡片。
- 扩大 Codex 日志初始化回读范围并保留最近对话映射，程序重启后重新聚焦 Codex 窗口即可恢复该窗口历史累计 Token。
- 前端、Rust、Tauri 及锁文件版本统一升级至 `1.4.0`。

### 验证

- 前端测试、TypeScript/Vite 构建、Rust 检查及 Windows Tauri release 构建通过。
- 真实 Windows Tauri 程序确认主卡片同时显示“当前窗口使用量”和“累计使用”，且不再创建额外 Token 浮窗。

## 1.3.1 — 2026-07-13

### 修复

- 预览窗或调色盘处于打开状态时，点击应用外区域会退出预览、关闭两个控制窗，并将主悬浮窗收回 `100×100`。
- 主窗口、预览窗和调色盘之间的内部焦点切换仍保持展开，避免正常操作滑块、色卡或按钮时误收起。

### 变更

- 前端、Rust、Tauri 及锁文件版本统一升级至 `1.3.1`。

### 验证

- 前端测试、TypeScript/Vite 构建、Rust 检查及 Windows Tauri release 构建通过。
- 真实 Windows Tauri 程序完成三个应用窗口内部切换保持展开，以及预览状态下点击应用外自动退出并收起验证。

## 1.3.0 — 2026-07-13

### 新增

- 调色盘编辑窗口的背景主题会跟随当前预览额度实时变化，不再固定显示在 `50%`。
- 主悬浮卡片在焦点移至应用外部后自动收回 `100×100` 最小窗口。

### 变更

- 原生失焦事件增加短暂复核，焦点切换到色彩预览或调色盘编辑窗口时保持展开，避免应用内部操作误触发收起。
- 前端、Rust、Tauri 及锁文件版本统一升级至 `1.3.0`。

### 验证

- 前端测试、TypeScript/Vite 构建、Rust 检查及 Windows Tauri release 构建通过。
- 浏览器与真实 Windows Tauri 程序完成调色盘动态背景、应用内窗口切换保持展开及应用外失焦自动收起检查。

## 1.2.0 — 2026-07-13

### 新增

- 折叠悬浮窗与完整额度卡片改为同一 DOM 的 shared-layout morph：固定窗口锚点，连续过渡主数字、背景、圆角与阴影，并为卡片信息增加约 40ms 的错峰入场。
- 色彩预览与调色盘增加分阶段开关动画、五色块错峰、滑块圆点回弹及百分比平滑滚动。

### 变更

- 鼠标离开后延迟 120ms 收起；快速移入移出、快速开关调色窗口和动画中连续点击由可取消时序保护，避免闪烁与窗口残留。
- 调色窗口按预览先开、调色盘后开，关闭时反序且更快；退场结束后才隐藏原生窗口。
- Vite 开发服务忽略 Rust `target` 构建目录，避免 Windows DLL 链接期间触发 `EBUSY`。
- 所有新增动效支持 `prefers-reduced-motion`；前端、Rust、Tauri 及锁文件版本统一升级至 `1.2.0`。

### 验证

- 前端 17 项测试、TypeScript/Vite 构建、Rust 检查及 Windows Tauri release 构建通过。
- 浏览器完成 `100×100`、`320×320`、`320×104`、关键颜色、百分比滚动和减弱动画检查。
- 真实 Windows Tauri 程序完成三态切换、快速移入移出、连续开关、完成退出、双窗口跟随及屏幕底部上方避让验证。

## 1.1.0 — 2026-07-13

### 新增

- 会话 Token 浮窗支持直接拖动；拖动后保留相对 Codex 窗口的新位置，并继续随 Codex 窗口移动。

### 变更

- 移除 Token 浮窗的胶囊背景、边框、阴影和模糊效果，仅保留紧凑 Token 文字。
- 前端、Rust、Tauri 及锁文件版本统一升级至 `1.1.0`。

### 验证

- 前端 17 项测试、TypeScript/Vite 构建、Rust 检查及 Windows Tauri release 构建通过。
- 在真实 Windows Codex 窗口完成拖动保持和拖动后相对位置跟随验证。

## 1.0.0 — 2026-07-13

基线：上游 `v0.1.3`（`27943c0`）。以下内容构成 Quota Beacon `1.0.0` 的首个独立开发版本。

### 新增

- 本机 Codex 历史 Token 汇总与会话 Token 浮层显示。
- 101 级额度颜色主题：五个可编辑关键色、连续插值、对比度处理，以及防止相邻整数百分比重色的自动测试。
- 独立色彩预览窗口与调色盘编辑窗口；支持 `0–100%` 滑块预览、恢复默认色和保存自定义色。

### 变更

- 主卡片和折叠悬浮球改用同一套额度主题算法；以 5 小时额度为主视觉，缺少 5 小时数据时回退显示周额度。
- 真实额度改为每 10 秒强制刷新；预览期间同步显示滑块所选额度，退出后恢复真实数据。
- 增强双窗口协作、移动跟随、可用工作区上下避让、高 DPI 物理尺寸处理，以及预览期间的置顶状态恢复。
- 为额度数据、设置加载、Token 格式化和中英文文案补齐类型、桥接和测试覆盖。
- 进度条末端使用无实体的白色柔光提示当前位置，避免圆形标记造成“可拖动按钮”的错误暗示。
- 前端、Rust 与 Tauri 构建版本统一提升为 `0.1.4`，用于区分包含上述功能的可恢复版本。

## 未版本化开发里程碑 — 2026-07-12

### 修复色卡预览透明边缘

- 移除透明色卡窗口边缘被裁切后形成灰色矩形的外部阴影。
- 完成前端测试、Vite 构建、Rust 检查和 Tauri 桌面构建；重建并替换本机安装程序进行验证。

### 修复设置错误提示残留

- 设置读取在启动阶段短暂失败时，错误横幅曾在后续额度正常更新后仍然保留。
- 增加设置读取重试，并在设置读取或桌面设置同步成功后清除过期错误状态。

## 上游发布基线

| 版本 | 提交 | 日期 | 内容 |
| --- | --- | --- | --- |
| `v0.1.0` | `cc395ff` | 2026-07-09 | 初始发布：Tauri/React 悬浮额度工具、托盘、偏好、构建与发布流程。 |
| `v0.1.1` | `d122350` | 2026-07-09 | 修复 macOS Release 工件压缩路径。 |
| `v0.1.2` | `3b25c07` | 2026-07-09 | 发布配置统一升至 `0.1.2`，并调整 Release 工作流。 |
| `v0.1.3` | `27943c0` | 2026-07-09 | Windows Release 构建隐藏控制台窗口。 |

## 记录规则

- 下一次对外发布前，为当前快照分配新版本号、创建提交并打 Git 标签。
- 后续每个版本在此记录新增、变更、修复和验证结果。
- 上游来源、许可与当前衍生开发的边界见 [UPSTREAM-NOTICE.md](UPSTREAM-NOTICE.md)。
