# 开发历程与变更日志

本文件记录本仓库可核实的版本与开发里程碑。未创建 Git 提交或标签的工作统一标记为“未发布”，不追溯虚构版本号。

## macOS 1.9.9 — 2026-08-18

### CI 安全门禁修复

- 更新 Vite 开发链的间接依赖：`postcss` 升至 `8.5.26`，`nanoid` 升至 `3.3.18`。
- 消除 GitHub Actions `npm audit --audit-level=high` 报告的两项高危依赖告警；不升级 Vite 或修改应用运行逻辑。

## macOS 1.9.8 — 2026-08-16

### 产品线硬隔离

- 新增机器可读的 `PRODUCT_LINE.json` 与跨平台预检脚本，修改、测试和提交前会核对 macOS 产品线、`macos` 分支、`macos-v*` 标签规则、六处版本源及 Mac 原生配置。
- 修正 Mac 工作树的 `AGENTS.md`，移除 Windows 标签、推送、实机验收和原生实现指令；Windows `main` 仅保留为只读功能参考。
- 预检允许 `macos` 和 `codex/macos-*`，明确拒绝 Windows 目标、错误分支、缺失 Keychain/透明窗口基座或混乱版本源。

### 范围边界

- 本版本只加固开发与发布前的产品线识别，不修改额度、账号、窗口或视觉运行逻辑。
- Windows `main` 的对应保护在其独立工作树和独立版本中实现；本版本不合并或复制 Windows 平台代码。

## macOS 1.9.7 — 2026-08-13

### Mac 追赶计划收尾

- 将 macOS 追赶计划从待执行方案收口为本地完成记录，确认阶段 A、C、D、E 已完成，阶段 B 按用户决定取消且不再补做。
- 记录 C1、C2、D1、C3、A1、R1 在 `macos-v1.9.1` 至 `macos-v1.9.6` 的实际落点、已完成的自动验证和仍待真实 macOS 验证的边界。
- 将 A/C/D 修复计划归档为执行记录，避免后续会话按旧的 `macos-v1.9.0` 基线重复启动修复。

### 范围边界

- 本版本仅更新版本元数据、变更日志和收尾文档，不修改 C1/C2/D1/C3 或其他功能源码。
- Windows `main` 产品线未修改；真实 macOS CI、Universal 最终工件、Keychain 系统交互与真机视觉仍需独立验证。

## macOS 1.9.6 — 2026-08-13

### R1 已证实冗余收敛

- 删除账号切换结果中始终固定且前端从未消费的 `credentialsSwitched` 与 `restartRecommended`；切换结果只保留实际使用的账号资料。
- 收窄账号周额度状态类型，移除 Rust 后端从不产生的 `stale`；主卡快照的 stale 恢复语义保持不变。
- 非当前账号周额度改为真正的 usage/weekly-only 路径，不请求 reset-credits，也不解析或返回 5 小时额度、plan、reset credits 数据。
- HTTP User-Agent 集中为 `Quota-Beacon/<构建版本>`，随 Cargo 版本自动同步；移除旧的固定 `QuotaFloat/0.1`。
- 保留 `AccountVault::view() -> Result`：C1/C2 已引入真实损坏状态错误路径，该项不再是冗余候选。

### 范围边界

- 未处理需用户确认的 `?designer`/假数据、Windows 同源码构建代码与依赖、移动端图标或其他生成资产。
- 本版本未推送、未发布；真实 Mac 额度请求、Keychain 与最终 Universal 工件仍需 macOS CI 和真机验收。

## macOS 1.9.5 — 2026-08-13

### Mac 发布链加固

- 发布标签严格限制为 `macos-v<semver>`，并验证标签版本、七处源码版本、标签提交与 Mac 产品线历史一致。
- Universal bundle 必须恰好包含一个 `.app` 和一个 DMG；最终 App 验证 ad-hoc 签名、`arm64 + x86_64`、两项 bundle 版本和主程序可执行权限。
- 在 macOS runner 使用 `ditto` 生成保留权限、符号链接和 bundle 结构的 ZIP，并在全新临时目录解包后重新验证最终 App。
- 最终发布工件固定为 DMG、ZIP 及各自独立的 SHA-256 四件套；上传和草稿 Release 均使用显式白名单，不再吞入目录中的额外文件。
- 发布脚本的正反例在 CI 中实际执行；前端配置单元测试不再用字符串包含断言替代 YAML 和 shell 语义验证。

### 验证边界

- Windows 本机可验证版本脚本、失败保护、前端/Rust 构建和脚本语法；Universal App、ad-hoc 签名、DMG、ZIP 解包和最终校验链仍需 macOS CI 实际运行。
- 本版本未推送标签、未上传工件、未创建或发布 Release。

## macOS 1.9.4 — 2026-08-13

### C3 账号窗口状态与登录轮询

- 账号窗口使用可测试的物理坐标定位函数同时夹紧 x/y，支持右边缘、左边缘、负坐标副屏、Dock/菜单栏工作区以及上下空间不足的窗口高度变化。
- 所有隐藏路径统一进入同一个原生函数并且只发出一次 closed 状态；opened 只在 `show()`、定位和账号视图读取成功后发出，后续失败会隐藏窗口并回滚生命周期状态。
- 重开窗口时清理已结束的新增表单、别名和旧提示；浏览器中的运行中登录任务继续保留，并重新显示登录提示与取消入口。
- 750ms 登录轮询改为单一在途的串行超时链；后端保留最近完成结果，使相同 task ID 的重复轮询幂等返回 completed/failed，而不是误报没有登录任务。
- 账号窗口尺寸动画增加代次取消，快速展开/收起时旧动画不能覆盖最新目标尺寸。

### 自动验证

- Rust 覆盖左右边缘、负坐标显示器、上下避让、窄工作区夹紧和登录完成状态幂等；前端覆盖重开状态、慢轮询单在途、运行中登录恢复显示和旧尺寸动画取消。

### 验证边界

- Windows 本机可验证纯定位、事件状态、轮询和尺寸取消逻辑；Retina/外接显示器、负坐标真实排列、Dock/菜单栏、浏览器登录失焦和透明窗口仍需真实 Mac 验收。
- 本版本未修改 Windows `main` 产品线，未推送、未上传、未创建 Release。

## macOS 1.9.3 — 2026-08-13

### D1 周额度生命周期与缓存代次

- 账号窗口只在原生窗口成功显示并发出 `account-switcher-opened` 后立即查询周额度；关闭、托盘隐藏、主窗口关闭和单实例唤醒清理统一发出 closed 状态，隐藏期间不再进行周期性 Keychain 读取或非当前账号额度请求。
- 非当前账号使用独立凭据代次与缓存代次；删除、重新登录、保存或切换账号会使旧请求失效，关闭窗口后的在途响应不会写入缓存或界面。
- 成功额度缓存 5 分钟，登录失效只短暂缓存，网络失败、429 与服务不可用不缓存 5 分钟；多账号请求限制为最多 3 个并发，单账号失败不阻塞其他账号。
- 非当前账号只请求 usage 端点，不额外访问 reset-credits；当前账号快照按 `provider == "codex"` 查找，不依赖返回顺序。

### 自动验证

- 前端覆盖隐藏窗口不查询、打开立即查询和关闭停止更新；Rust 覆盖 299/300 秒缓存边界、临时失败不缓存、旧凭据响应失效、Codex 快照乱序以及 weekly-only 端点选择。

### 验证边界

- Windows 本机可验证共享 TypeScript/Rust 逻辑、前端构建和宿主编译；真实 Mac Keychain、断网/429 恢复、多账号并发时序与隐藏窗口网络静默仍需 macOS CI 和真机验收。
- 本版本未修改 Windows `main` 产品线，未推送、未上传、未创建 Release。

## macOS 1.9.2 — 2026-08-13

### C2 当前账号与重新登录一致性

- 每次生成账号视图前读取真实 `auth.json` 指纹：匹配已保存账号才标记为当前；未保存、缺失或损坏登录均清除旧 `activeProfileId`，并且只在状态实际变化时持久化。
- 当前账号重新登录采用 Keychain、`auth.json` 和账号元数据三段事务；替换后验证真实文件，失败时恢复三份旧状态。非当前账号重新登录不修改当前 `auth.json`。
- 重新登录、删除或外部身份变化会增加账号代次并使旧额度请求失效；当前账号重新登录还会清空主快照并触发真实刷新。
- Keychain 条目缺失的非当前账号可直接删除；条目缺失、钥匙串锁定、权限拒绝、数据损坏和服务不可用使用不同状态，不再一律提示重新登录。
- 隔离登录目录在 macOS/Unix 上设为 `0700`，登录生成的临时 `auth.json` 在读取前收紧为 `0600`；重新登录成已存在账号继续拒绝重复身份。

### 自动验证

- Rust 覆盖外部切换至已保存/未保存账号、登录缺失或损坏、当前与非当前重新登录、三份状态回滚、重复身份拒绝、缺失 Keychain 删除和权限拒绝区分。
- 前端覆盖钥匙串锁定时禁用切换、保留删除入口且不误显示“重新登录”；全量前端与 Rust 回归继续覆盖窗口、配色、周额度和快照逻辑。

### 验证边界

- Windows 本机可验证共享 TypeScript/Rust 逻辑、前端构建、宿主编译和 Windows 无 bundle 构建；真实 macOS Keychain 锁定/授权提示、文件权限、双账号交互和 Universal 包仍需 Mac CI 与真机验收。
- 本版本未修改 Windows `main`，未推送、未上传、未发布。

## macOS 1.9.1 — 2026-08-13

### C1 账号库与凭据文件安全

- `vault.json` 主文件缺失或损坏时从有效 `.bak` 原子恢复；主文件和备份都无效时保留原文件并阻止静默清空或覆盖账号库。
- 加载和提交前拒绝重复 profile ID、重复账号指纹以及悬空 `activeProfileId`，避免元数据异常被当成有效状态。
- 元数据与 `auth.json` 使用随机唯一、`create_new` 的同目录临时文件；macOS/Unix 显式设为 `0600`，并在写入、同步或替换失败时清理临时文件。
- Mac 上直接原子替换正式文件并维护 last-known-good 备份；凭据或 Keychain 回滚失败会返回高优先级错误，不再误报“已恢复”。

### 自动验证

- Rust 覆盖主文件正常加载、主文件缺失/损坏时备份恢复、双损坏保留、元数据唯一性校验、预置临时路径不被覆盖和回滚失败语义。
- Unix/macOS 专项测试覆盖元数据与最终 `auth.json` 的 `0600` 权限；Windows 本机仅能验证共享文件逻辑和宿主编译，真实 Mac 文件系统与 Keychain 仍需 Mac CI/真机验收。

### 交付边界

- 本批只修复 C1，不包含 C2 当前账号对账与重新登录事务；未修改 Windows `main`，未推送、未发布。

## macOS 1.9.0 — 2026-08-13

### Mac 账号主题与视觉收口

- 账号窗口外壳跟随当前激活 Codex 账号的真实 5 小时剩余额度与用户调色盘；额度未知或账号切换等待新快照时使用中性玻璃色，不沿用旧账号色调。
- 从主卡与托盘打开账号窗口时均传递同一主题；窗口打开期间，真实额度或调色盘变化会同步更新色调。
- 账号列表保持中性，只有当前账号行使用弱状态染色；账号窗口继续使用 macOS 透明窗口与 WebKit 模糊，不引入 Windows DWM、Win32 或 Host Backdrop 实现。
- 共享颜色语义收敛到 `0% / 20% / 35% / 60% / 100%` 五个关键节点，`60%` 明确保持浅绿色，并继续保证 101 个整数百分比颜色互不重复。
- 为减少动态效果和减少透明度偏好提供降级，保留深色文字在红色低额度状态下的可读性。

### 自动验证

- 前端覆盖账号窗口真实主题、未知额度中性回退、五个关键色标、玻璃色变量和 101 色唯一性。
- Rust 侧规范化百分比与调色盘，并支持主卡和托盘打开路径的统一主题事件。

### 验证边界

- Windows 本机可验证共享 TypeScript/Rust 逻辑、前端构建、宿主编译和 Windows 无 bundle 构建；macOS Universal 构建、Retina/外接显示器、Dock/菜单栏避让、透明模糊与真实账号切换后的视觉一致性仍需 Mac CI 和真实 Mac 验收。
- 本版本未修改 Windows `main` 产品线，未推送、未上传、未创建 Release。

## macOS 1.8.0 — 2026-08-13

### Mac 多账号周额度

- 账号窗口为每个已保存账号只显示四舍五入后的每周剩余额度，不重复显示 5 小时额度、相对更新时间或过期标签。
- 当前账号复用主卡每 10 秒更新的真实快照；非当前账号直接使用各自 macOS Keychain 凭据查询，不切换或覆盖当前 `auth.json`。
- 非当前账号额度按随机 profile ID 严格缓存 5 分钟；删除账号、重新登录或切换为当前账号时清理对应缓存，不影响其他账号的缓存周期。
- 单个账号凭据缺失、身份不匹配或额度接口返回登录失效时，只将该账号标记为“重新登录”，其他账号仍可查看和切换。
- 切换成功提示在 10 秒后自动消失，并使用提示 ID 防止旧计时器清除后续错误。

### 自动验证

- 前端覆盖周额度取整、仅周额度展示、单账号失效隔离和提示计时器竞态。
- Rust 覆盖 Keychain 单账号凭据读取不修改 `auth.json`、5 分钟精确缓存边界以及缺少周窗口时不误用 5 小时额度。

### 验证边界

- Windows 本机可验证共享 TypeScript/Rust 逻辑与宿主构建；Mac Universal CI、真实 Keychain 查询、单/双/多账号额度一致性和账号窗口视觉仍需 macOS 环境验收。
- 阶段 C 的真实 Mac 双账号切换、Retina/多显示器行为仍是前置验收缺口；本版本未修改 Windows `main` 产品线。

## macOS 1.7.0 — 2026-08-13

### Mac 原生多账号

- 使用 macOS `Security.framework` Keychain 保存 Codex 账号凭据；磁盘元数据只包含随机账号 ID、别名、脱敏邮箱、账号指纹和当前账号 ID，前端不接收 Token 或完整账号 ID。
- 新增保存当前账号、隔离执行官方 `codex login`、添加、重新登录、重命名、删除和普通切换；不读取浏览器 Cookie，也不提供会中断 Codex 任务的自动重启入口。
- 切换前同步当前凭据并校验目标身份，使用同目录临时文件替换 `auth.json`；替换或元数据提交失败时恢复原登录。
- 新增账号代次隔离，切换后立即清空旧额度缓存，拒绝旧账号的在途响应覆盖新账号界面。

### 账号入口与窗口

- 主卡新增账号胶囊，托盘新增账号子菜单与管理入口；再次点击账号胶囊、点击主卡其他区域、关闭账号窗或应用失焦均可收回账号窗。
- 新增 `account-switcher` Mac 透明窗口，跟随主窗口移动，并依据当前显示器可见工作区自动选择显示在上方或下方。
- 账号窗默认折叠添加表单，支持键盘焦点、减少动态效果、脱敏身份信息和 Keychain 状态提示。

### 自动验证

- Rust 覆盖磁盘无凭据、重复账号、无效/超大认证、活动账号删除保护、原子切换、元数据失败回滚和旧账号响应隔离；Unix/macOS CI 额外拒绝符号链接认证文件。
- 前端覆盖账号胶囊与账号窗的脱敏展示和折叠表单；Mac 配置测试冻结 Keychain 依赖、稳定 service 名称和第四个透明窗口。

### 验证边界

- Windows 本机只验证共享 TypeScript/Rust 逻辑与宿主构建。Keychain 系统提示、官方登录启动路径、Retina/多显示器窗口行为和真实账号切换仍需 macOS Universal CI 与真实 Mac 验收。
- 本版本按用户指示跳过阶段 B；请求退避和分叉会话 Token 正确性不包含在本次 Mac 版本中。

## macOS 1.5.7 — 2026-08-12

### 回归保护

- 扩展 macOS 配置测试，统一校验 `VERSION`、npm、Cargo 与 Tauri 的 Mac 版本号，避免独立产品线发布时版本漂移。
- 冻结 `macos-v*` 唯一发布触发条件、Mac-only runner 与工件边界，并覆盖 Universal `arm64 + x86_64` 构建目标。
- 冻结 ad-hoc 签名、DMG 完整性、SHA-256 和草稿 Release 校验链，防止后续追赶 Windows 能力时破坏 Mac 发布基线。

### 验证边界

- Windows 本机可执行前端测试、TypeScript/Vite 构建、Rust 检查与 Rust 测试；macOS Universal 构建及 bundle 校验仍需 Mac CI。
- 真实 Mac 浅色/深色桌面上的折叠球和展开卡片四角验收仍未执行，不以源码或 Windows 构建替代。

## macOS 1.5.6 — 2026-08-01

### 修复

- 从 `macos-v1.5.5` 建立独立 macOS 版本线，Windows 的 `pro5x-test` 与 `v1.5.8.pro5x` 保持不变。
- 启用 Tauri `macOSPrivateApi`，并将 `widget`、`palette`、`palette-editor` 三个透明窗口的原生背景显式设为 `#00000000`，修复 macOS 悬浮球外侧及展开卡片四角出现白色方形底的问题。
- 新增配置回归测试，防止透明窗口遗漏原生透明背景。

### 发布

- macOS 使用独立 `macos` 分支与 `macos-v*` 标签，只生成 Universal ZIP、DMG 和 SHA-256；不再与 Windows 版本合并发布。

### 验证

- Windows 本机执行 22 项前端测试、10 项 Rust 测试、TypeScript/Vite 构建、Rust 检查和 Tauri release 构建；macOS Universal 构建、签名、双架构与 DMG 完整性由 GitHub Actions 验证。
- macOS 白底消失仍需使用本次 DMG 在真实 Mac 的浅色和深色桌面上验收。

## 1.5.5 — 2026-07-26

### 修复

- 历史累计 Token 扫描新增 `.codex/session-archive`，避免 Codex 将会话迁移到二级归档后累计值下降。
- 新增会话文件迁移回归测试，确保迁移前后的累计 Token 和会话数量保持一致。

### 验证

- Rust 单元测试覆盖 `sessions` 到 `session-archive/stage2/...` 的迁移场景，迁移前后的累计 Token 和会话数量断言通过。
- 前端测试、TypeScript/Vite 构建、Rust 检查、Windows Tauri release 构建与 Git 差异检查通过。

## 1.5.4 — 2026-07-17

### 文档

- 按用户要求删除已被两张新产品图替换、且不再被仓库引用的旧 `docs/images/quota-states.png`。
- 删除前已在忽略提交的 `work/backups/` 中创建可恢复备份，并记录原路径、目的、备份路径、时间与 SHA-256。

### 验证

- 删除前原文件与备份 SHA-256 一致；README 中两张新图片路径有效。
- 前端测试、TypeScript/Vite 构建、Rust 检查与 Git 差异检查通过。

## 1.5.3 — 2026-07-17

### 文档

- 将 README 首页原有的三状态截图替换为新的产品主视觉和 101 级额度配色系统展示图。
- 保留原始展示图片文件，避免破坏历史引用。

### 验证

- 两张新图片与用户提供的源文件 SHA-256 完全一致，README 路径有效。
- 前端测试、TypeScript/Vite 构建、Rust 检查与 Git 差异检查通过。

## 1.5.2 — 2026-07-17

### 维护

- 将 GitHub 官方 checkout、Node 环境和工件上传/下载 Actions 升级到基于 Node.js 24 的当前主版本，并继续固定到完整提交 SHA，消除 Node.js 20 弃用警告。

### 验证

- `v1.5.1` 主分支 CI 的前端、Windows 构建、macOS Universal 构建、ad-hoc 签名、双架构与 DMG 校验全部通过；本补丁再次由主分支 CI 验证新版 Actions。

## 1.5.1 — 2026-07-17

### 开源发布

- 更新项目首页、安装步骤、平台验证状态、真实截图、非官方声明和私密安全报告入口。
- 首次推送流程改为分别推送 `main` 与单个版本标签，避免历史标签批量触发 Release。
- Release 改为等待 Windows 与 macOS 构建全部成功后，在单一草稿中汇总 Windows ZIP、macOS Universal ZIP、DMG 和 SHA-256。
- GitHub Actions 权限按最小范围设置，所有外部 Actions 固定到完整提交 SHA，并新增 Dependabot、CODEOWNERS 与安全 Issue 引导。
- MIT 版权声明同时保留上游贡献者与 `suzyac5318 / Quota Beacon contributors`。

### 验证范围

- Windows 11 为已完成实机验证的平台。
- macOS Universal 包由 GitHub Actions 构建并自动验证双架构、ad-hoc 签名与 DMG 完整性；真实 Mac 桌面交互仍明确标记为待验收。

## 1.5.0 — 2026-07-17

### 新增

- macOS Universal 构建改用无需付费开发者账号的 ad-hoc 签名，同时保留 Apple Silicon 与 Intel 双架构。
- Release 直接提供 DMG、完整 bundle ZIP 和 SHA-256 校验文件。
- 新增 macOS 自动验收脚本，检查递归签名、ad-hoc 身份、双架构主程序和 DMG 完整性。

### 变更

- CI 与 Release 工件统一改名为 `quota-beacon-macos-universal-ad-hoc`，明确其未公证的分发边界。
- 安装说明补充 Gatekeeper 的安全放行流程、校验值用法和 Mac 实机验收要求。

### 验证

- 前端测试、TypeScript/Vite 构建、Rust 检查和 Windows Tauri 构建通过。
- macOS Universal 架构、ad-hoc 签名与 DMG 校验由 macOS CI 执行；真实 Mac 首次安装与桌面交互待发布工件生成后验收。

## 1.4.3 — 2026-07-17

### 修复

- 收窄色彩预览标题区的说明文字样式选择器，避免动态百分比数字被错误缩小为 `10px`，使数字与百分号统一保持清晰的 `20px` 显示。

### 验证

- 新增 CSS 选择器回归测试，防止说明文字样式再次影响动态百分比。

## 1.4.2 — 2026-07-14

### 变更

- 将应用图标改为透明背景的明亮玻璃质感「Q」轨道，采用蓝、靛、紫、粉多彩渐变与柔和外发光。
- 重新生成 Windows、macOS、iOS 与 Android 所需的全部应用图标尺寸。

## 1.4.1 — 2026-07-14

### 优化

- 将卡片背景 Morph 与文字动画彻底分离，小窗百分比和完整卡片内容保持独立且始终挂载，文字只使用透明度、位移和轻微模糊过渡。
- 展开时背景先行，小窗与大卡片百分比交叉淡入淡出，标题、累计使用、进度条和底部信息按约 `30ms` 错峰出现。
- 收回时次要信息先退出，大百分比向小窗锚点轻移后与小窗百分比交叉切换，避免文字随容器收缩而挤压、换行或跳变。
- 快速移入移出可自然反向播放；保留现有卡片背景 Morph、视觉样式和减少动态效果支持。

### 验证

- 新增组件结构测试，确认折叠与展开内容在方向反转时不会重复挂载。
- 前端测试、TypeScript/Vite 构建、Rust 检查、Windows Tauri release 构建及真实桌面交互验证通过。

## 1.4.0 — 2026-07-13

### 新增

- 主悬浮卡片在“累计使用”上方新增“当前窗口使用量”，自动显示当前 Codex 对话窗口累计 Token，缺失时显示 `null`。

### 变更

- 删除独立的当前窗口 Token 浮窗及其窗口创建、定位和拖动逻辑，当前窗口数据改为直接发送至主悬浮卡片。
- 扩大 Codex 日志初始化回读范围并保留最近对话映射，程序重启后重新聚焦 Codex 窗口即可恢复该窗口历史累计 Token。
- 前端、Rust、Tauri 及锁文件版本统一升级至 `1.4.0`。

### 验证

- 前端测试、TypeScript/Vite 构建、Rust 检查及 Windows Tauri release 构建通过。
- 真实 Windows Tauri 程序确认主卡片同时显示“当前窗口使用量”和“累计使用”，且不再创建额外 Token 浮窗。

## 1.3.1 — 2026-07-13

### 修复

- 预览窗或调色盘处于打开状态时，点击应用外区域会退出预览、关闭两个控制窗，并将主悬浮窗收回 `100×100`。
- 主窗口、预览窗和调色盘之间的内部焦点切换仍保持展开，避免正常操作滑块、色卡或按钮时误收起。

### 变更

- 前端、Rust、Tauri 及锁文件版本统一升级至 `1.3.1`。

### 验证

- 前端测试、TypeScript/Vite 构建、Rust 检查及 Windows Tauri release 构建通过。
- 真实 Windows Tauri 程序完成三个应用窗口内部切换保持展开，以及预览状态下点击应用外自动退出并收起验证。

## 1.3.0 — 2026-07-13

### 新增

- 调色盘编辑窗口的背景主题会跟随当前预览额度实时变化，不再固定显示在 `50%`。
- 主悬浮卡片在焦点移至应用外部后自动收回 `100×100` 最小窗口。

### 变更

- 原生失焦事件增加短暂复核，焦点切换到色彩预览或调色盘编辑窗口时保持展开，避免应用内部操作误触发收起。
- 前端、Rust、Tauri 及锁文件版本统一升级至 `1.3.0`。

### 验证

- 前端测试、TypeScript/Vite 构建、Rust 检查及 Windows Tauri release 构建通过。
- 浏览器与真实 Windows Tauri 程序完成调色盘动态背景、应用内窗口切换保持展开及应用外失焦自动收起检查。

## 1.2.0 — 2026-07-13

### 新增

- 折叠悬浮窗与完整额度卡片改为同一 DOM 的 shared-layout morph：固定窗口锚点，连续过渡主数字、背景、圆角与阴影，并为卡片信息增加约 40ms 的错峰入场。
- 色彩预览与调色盘增加分阶段开关动画、五色块错峰、滑块圆点回弹及百分比平滑滚动。

### 变更

- 鼠标离开后延迟 120ms 收起；快速移入移出、快速开关调色窗口和动画中连续点击由可取消时序保护，避免闪烁与窗口残留。
- 调色窗口按预览先开、调色盘后开，关闭时反序且更快；退场结束后才隐藏原生窗口。
- Vite 开发服务忽略 Rust `target` 构建目录，避免 Windows DLL 链接期间触发 `EBUSY`。
- 所有新增动效支持 `prefers-reduced-motion`；前端、Rust、Tauri 及锁文件版本统一升级至 `1.2.0`。

### 验证

- 前端 17 项测试、TypeScript/Vite 构建、Rust 检查及 Windows Tauri release 构建通过。
- 浏览器完成 `100×100`、`320×320`、`320×104`、关键颜色、百分比滚动和减弱动画检查。
- 真实 Windows Tauri 程序完成三态切换、快速移入移出、连续开关、完成退出、双窗口跟随及屏幕底部上方避让验证。

## 1.1.0 — 2026-07-13

### 新增

- 会话 Token 浮窗支持直接拖动；拖动后保留相对 Codex 窗口的新位置，并继续随 Codex 窗口移动。

### 变更

- 移除 Token 浮窗的胶囊背景、边框、阴影和模糊效果，仅保留紧凑 Token 文字。
- 前端、Rust、Tauri 及锁文件版本统一升级至 `1.1.0`。

### 验证

- 前端 17 项测试、TypeScript/Vite 构建、Rust 检查及 Windows Tauri release 构建通过。
- 在真实 Windows Codex 窗口完成拖动保持和拖动后相对位置跟随验证。

## 1.0.0 — 2026-07-13

基线：上游 `v0.1.3`（`27943c0`）。以下内容构成 Quota Beacon `1.0.0` 的首个独立开发版本。

### 新增

- 本机 Codex 历史 Token 汇总与会话 Token 浮层显示。
- 101 级额度颜色主题：五个可编辑关键色、连续插值、对比度处理，以及防止相邻整数百分比重色的自动测试。
- 独立色彩预览窗口与调色盘编辑窗口；支持 `0–100%` 滑块预览、恢复默认色和保存自定义色。

### 变更

- 主卡片和折叠悬浮球改用同一套额度主题算法；以 5 小时额度为主视觉，缺少 5 小时数据时回退显示周额度。
- 真实额度改为每 10 秒强制刷新；预览期间同步显示滑块所选额度，退出后恢复真实数据。
- 增强双窗口协作、移动跟随、可用工作区上下避让、高 DPI 物理尺寸处理，以及预览期间的置顶状态恢复。
- 为额度数据、设置加载、Token 格式化和中英文文案补齐类型、桥接和测试覆盖。
- 进度条末端使用无实体的白色柔光提示当前位置，避免圆形标记造成“可拖动按钮”的错误暗示。
- 前端、Rust 与 Tauri 构建版本统一提升为 `0.1.4`，用于区分包含上述功能的可恢复版本。

## 未版本化开发里程碑 — 2026-07-12

### 修复色卡预览透明边缘

- 移除透明色卡窗口边缘被裁切后形成灰色矩形的外部阴影。
- 完成前端测试、Vite 构建、Rust 检查和 Tauri 桌面构建；重建并替换本机安装程序进行验证。

### 修复设置错误提示残留

- 设置读取在启动阶段短暂失败时，错误横幅曾在后续额度正常更新后仍然保留。
- 增加设置读取重试，并在设置读取或桌面设置同步成功后清除过期错误状态。

## 上游发布基线

| 版本 | 提交 | 日期 | 内容 |
| --- | --- | --- | --- |
| `v0.1.0` | `cc395ff` | 2026-07-09 | 初始发布：Tauri/React 悬浮额度工具、托盘、偏好、构建与发布流程。 |
| `v0.1.1` | `d122350` | 2026-07-09 | 修复 macOS Release 工件压缩路径。 |
| `v0.1.2` | `3b25c07` | 2026-07-09 | 发布配置统一升至 `0.1.2`，并调整 Release 工作流。 |
| `v0.1.3` | `27943c0` | 2026-07-09 | Windows Release 构建隐藏控制台窗口。 |

## 记录规则

- 下一次对外发布前，为当前快照分配新版本号、创建提交并打 Git 标签。
- 后续每个版本在此记录新增、变更、修复和验证结果。
- 上游来源、许可与当前衍生开发的边界见 [UPSTREAM-NOTICE.md](UPSTREAM-NOTICE.md)。
